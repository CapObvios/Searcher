#pragma once
#include <set>
#include <string>
#include <map>
#include <vector>
#include <algorithm>
#include <iterator>

using std::string;
using std::set;
using std::vector;
using std::map;

#define NO_LIMITER_CODE -1
#define NO_LIMITER set<int>({NO_LIMITER_CODE})